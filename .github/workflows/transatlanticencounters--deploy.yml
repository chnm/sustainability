name: "Deploy Static Website -- transatlanticencounters"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'transatlanticencounters/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "transatlanticencounters"
      website-devl-fqdn: "transatlaenc.dev.chnm.gmu.edu"
      website-prod-fqdn: "transatlaenc.dev.chnm.gmu.edu"
      runner_labels: '["self-hosted","IncusOS"]'
