name: "Deploy Static Website -- thanksroy"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'thanksroy/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "thanksroy"
      website-devl-fqdn: "thanksroy.dev.chnm.gmu.edu"
      website-prod-fqdn: "thanksroy.dev.chnm.gmu.edu"
      runner_labels: '["self-hosted","IncusOS"]'
