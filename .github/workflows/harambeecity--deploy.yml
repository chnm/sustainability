name: "Deploy Static Website -- harambeecity"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'harambeecity/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "harambeecity"
      website-devl-fqdn: "static.rrchnm.org"
      website-prod-fqdn: "harambeecity.rrchnm.org"
