name: "Deploy Static Website -- islamperspectives"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'islamperspectives/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "islamperspectives"
      website-devl-fqdn: "islampers.dev.chnm.gmu.edu"
      website-prod-fqdn: "islampers.dev.chnm.gmu.edu"
      runner_labels: '["self-hosted","IncusOS"]'
