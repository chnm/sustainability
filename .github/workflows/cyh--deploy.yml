name: "Deploy Static Website -- cyh"
  
on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'cyh/**'
      - "!.github/**"

jobs: 
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "cyh"
      website-devl-fqdn: "static.rrchnm.org"
      website-prod-fqdn: "cyh.rrchnm.org"
