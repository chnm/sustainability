name: "Deploy Static Website -- plastercast"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'plastercast/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "plastercast"
      website-devl-fqdn: "plastercast.dev.chnm.gmu.edu"
      website-prod-fqdn: "plastercast.gmu.edu"
      runner_labels: '["self-hosted","IncusOS"]'
