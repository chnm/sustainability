name: "Deploy Static Website -- virginiaslostat"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'virginiaslostat/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "virginiaslostat"
      website-devl-fqdn: "valostat.dev.chnm.gmu.edu"
      website-prod-fqdn: "valostat.dev.chnm.gmu.edu"
      runner_labels: '["self-hosted","IncusOS"]'
