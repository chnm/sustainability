name: "Deploy Static Website -- dohistory"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'dohistory/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "dohistory"
      website-devl-fqdn: "static.rrchnm.org"
      website-prod-fqdn: "dohistory.org"
