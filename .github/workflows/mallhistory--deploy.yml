name: "Deploy Static Website -- mallhistory"

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - 'mallhistory/**'
      - "!.github/**"

jobs:
  deploy:
    uses: chnm/.github/.github/workflows/static--deploy.yml@main
    secrets: inherit
    with:
      context_root: "mallhistory"
      website-devl-fqdn: "mallhistory.dev.chnm.gmu.edu"
      website-prod-fqdn: "mallhistory.dev.chnm.gmu.edu"
      runner_labels: '["self-hosted","IncusOS"]'
